export const formatCurrency = (nrs) =>{
    let usd = Math.round((nrs/138),2);
    console.log(usd);
}

export default const generateRandomId = () =>{
    let random = Math.random();
    console.log(random);
}