export const PI = 3.1415
const calculateAreaOfCircle = (r) => PI * r * r;
export default calculateAreaOfCircle
const calculatePerimeterOfCircle = (r) => 2 * PI * r;
const calculateDiameter = (r) => 2 * r;
export {calculatePerimeterOfCircle, calculateDiameter}
